{
  "name": "rippyshreddy",
  "version": "1.0.0",
  "description": "",
  "main": "rippyshreddy.js",
  "scripts": {
    "build": "tsc rippyshreddy.ts --sourcemap --module commonjs --outDir build",
    "build-server": "tsc server.ts --sourcemap --module commonjs --outDir build",
    "watch": "watch 'npm run build'"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^1.5.0",
    "watch": "^0.16.0"
  },
  "dependencies": {
    "ws": "^0.7.2"
  }
}
